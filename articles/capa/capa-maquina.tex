% Capa inspirada no modelo fornecido, com ilustração conceitual de "máquina produzindo livros com pessoas"
% Requer: compilador XeLaTeX ou pdfLaTeX com TikZ

\documentclass[a4paper]{article}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usetikzlibrary{calc, shapes.geometric, positioning, decorations.pathmorphing, decorations.markings, decorations.text, shapes.symbols}
\usepackage{anyfontsize}
\usepackage{graphicx}

\pagestyle{empty}

\begin{document}

\begin{tikzpicture}[overlay,remember picture]
  % Fundo
  \fill[gray!5] (current page.south west) rectangle (current page.north east);

  % Cintas coloridas em segundo plano
  \shade[left color=Dandelion,right color=Dandelion!40,rotate around={45:($(current page.north west)+(0,-6)$)}]
    ($(current page.north west)+(0,-6)$) rectangle ++(9,1.5);

  \shade[left color=RoyalBlue,right color=Emerald,rotate around={45:($(current page.north west)+(13,-10)$)}]
    ($(current page.north west)+(13,-10)$) rectangle ++(15,1.5);

  % Máquina base
  \fill[gray!50,rounded corners=5pt] ($(current page.center)+(-4,-1)$) rectangle ++(8,3);
  \node at ($(current page.center)+(0,0.5)$) {\Large\bfseries Máquina de Conhecimento};

  % Engrenagens laterais
  \foreach \x in {-5.5, 5.5} {
    \node[draw, fill=gray!40, gear, minimum size=1.2cm] at ($(current page.center)+(\x,0.5)$) {};
  }

  % Pessoas com cabeca e corpo
  \foreach \x in {-2.5, -1, 0.5, 2} {
    \draw[fill=SkyBlue] ($(current page.center)+(\x,-2)+(0,0.5)$) circle (0.25); % cabeca
    \draw[fill=SkyBlue] ($(current page.center)+(\x,-2.5)$) rectangle ++(0.5,0.7); % corpo
  }

  % Esteira
  \draw[thick, fill=gray!30] ($(current page.center)+(4.4,-1.2)$) rectangle ++(4,0.4);
  \foreach \i in {0,...,5} {
    \draw[gray!70] ($(current page.center)+(4.4 + 0.66*\i,-1.2)$) -- ++(0,0.4);
  }

  % Livros sendo "produzidos"
  \foreach \i in {0,...,4} {
    \draw[fill=white,draw=black] ($(current page.center)+(4.5,-1)+(0,-\i*0.7)$) rectangle ++(1,0.5);
    \node[font=\tiny] at ($(current page.center)+(5,-1.25)+(-0.1,-\i*0.7)$) {Livro \#\i};
  }

  % Título
  \node[align=center] at ($(current page.north)+(0,-4cm)$)
    {\fontsize{50}{60}\selectfont \textbf{GESCONS} \\[0.5em]
     \fontsize{16}{20}\selectfont \textcolor{RoyalBlue}{\bf Revista da Associação Internacional Editares}};

  % Rodapé
  \node at ($(current page.south)+(0,2cm)$) {\includegraphics[width=3cm]{Logo-Editares-com-Marca-Registrada.png}};

  % Data
  \node[align=center] at ($(current page.center)+(0,6.5)$)
    {\fontsize{22}{26}\selectfont Número 7 -- Dezembro \\[0.2em]
     \fontsize{60}{72}\selectfont \textcolor{orange}{2025}};

\end{tikzpicture}

\end{document}